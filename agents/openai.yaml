interface:
  display_name: "Steven Wiki Skill"
  short_description: "Maintain a shared LLM-written wiki"
  default_prompt: "Use $steven-wiki-skill to check raw/wiki freshness, update wiki pages, and maintain the shared knowledge base."

policy:
  allow_implicit_invocation: true
